## 도메인 일지 하네스

`docs/domains/<도메인>.md`를 편집하기 **전에 그 페이지의 `일지` 절을 먼저 읽고**(`상태` + 최근 이력), 의미 있는 변경(데이터 구조·규칙·스키마·enum·관계·시점 등 "이렇게 하기로 했다") 후에는 `일지` 절에 항목 1개를 추가하고 `상태` 절을 갱신한다. 오타·링크 정리 같은 무가치 변경은 제외.
