---
domain: <english-slug>
status: not-started        # not-started → in-progress → done
pattern: <TS | DM>         # 트랜잭션 스크립트 / 도메인 모델
depends: []                # 의존하는 도메인 slug 목록 (예: [auth, billing])
# --- implement.ps1 백엔드용 (선택, Agent 병렬이면 비워둬도 됨) ---
branch: feat/<slug>
base: main
engine: codex              # codex | claude
---

# <도메인 한글 이름> (`<english-slug>`)

> 서브에이전트에게: **이 페이지에 적힌 것만 구현한다.** 규약 SoT = [ARCHITECTURE.md](../arch/ARCHITECTURE.md). 모호하면 추측 말고 멈추고 보고. 「경계(하지 말 것)」 밖 파일은 건드리지 않는다.

## 역할
*이 도메인이 무엇을 책임지는가. 한 단락.*
*[입력 필요: 도메인 책임 한 단락]*

## 구현목표 / 수용기준
- [ ] *[입력 필요: <조건>이면 <결과>가 된다]*
- [ ] *[입력 필요: <경계>이면 <결과>다]*

## 경계 (하지 말 것)
- 지정 범위 밖 파일 수정 금지. 재설계·리네이밍·겸사겸사 리팩터 금지. 새 의존 추가 금지.
- *[입력 필요: 이 도메인이 손대면 안 되는 영역]*

## 의존
*다른 도메인을 읽기/호출/구독으로 닿는 곳. 의존 방향 단방향 유지.*
| 대상 도메인 | 방식 (읽기/호출/구독) | 무엇을 |
|---|---|---|
| *[입력 필요]* | *[입력 필요]* | *[입력 필요]* |

## 패턴 (TS/DM + 근거)
*ARCHITECTURE §패턴 정책 기본값에서 출발. 어긋나면 근거.*
- 채택: <TS / DM> — *[입력 필요: 왜]*

<!-- 선택 절 — 프론트/백 디테일이 있는 도메인만 채운다
## 씬 (플로우·V)
*화면 전환·플로우 노드/간선·표현(V). 데이터 기능은 링크만.*

## 데이터 (M·C)
*소유 테이블/enum(M) + 기능별 처리: 입력·규칙·경계·출력(C). 값 의미는 [입력 필요].*
-->

## 상태
*이 도메인의 지금 상태 한 단락. 변경마다 갱신.*
not-started

## 일지
### YYYY-MM-DD — <결정 제목>
- **결정:** …
- **왜:** …
- **영향:** …
